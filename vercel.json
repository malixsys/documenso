{
  "version": 2,
  "buildCommand": "turbo run build --filter=@documenso/remix --output-logs=errors-only && echo '=== VERCEL DEBUG ===' && node --version && ls -la api/ && ls -la apps/remix/build/server/vercel.js && du -sh apps/remix/build/server/ && mkdir -p public && cp -r apps/remix/build/client/. public/ && echo '=== BUILD COMPLETE ==='",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "includeFiles": "apps/remix/build/server/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, immutable, max-age=31536000" }
      ]
    }
  ]
}
